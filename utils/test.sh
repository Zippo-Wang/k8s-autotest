#!/usr/bin/env bash

# for file in $(find ${operate2} -name *.${file_type} -type f); do\

# for的结果存入数组中，外层不需要再加小括号
# arr=$(find /mnt/d/DOCS/study/k8s-autotest/test-case/OBS -name pvc.yaml -type f)
# for x in $arr[@]; do echo $x; done;

echo "我的"
echo "hhh"
